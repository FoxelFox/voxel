{
  "scripts": {
    "build": "bun run build.ts",
    "dev": "bun run build.ts --dev"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@webgpu/types": "^0.1.64"
  },
  "dependencies": {
    "lit": "^3.3.1",
    "wgpu-matrix": "^3.4.0"
  }
}
